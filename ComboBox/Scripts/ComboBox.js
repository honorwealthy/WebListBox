/*! 
* interactListBox v1.0.0
* Copyright (c) 2015 Telexpress
* Released under the MIT license 
*/

; (function ($) {
    $.fn.interactListBox = function (setting) {
        var default_args = {
        }
        var _setting = $.extend(default_args, setting);

        return this.each(function () {
            var $this = $(this);
        });
    };

} (jQuery));